# Sign Up Screen

## Reference
Use `docs/references/auth-home-reference.png`.

## Target
Same visual system as Login with fields for name, phone, email, password, confirm password, terms checkbox, and green primary CTA.

## Rules
Reuse the same no-AppBar auth scaffold, real logo system, input style, button style, spacing, and footer link style as Login. Secondary auth screens show a compact inline back header inside the content.
